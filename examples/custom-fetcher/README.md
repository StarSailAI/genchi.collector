# allfeeds-example-fetcher

A minimal installable third-party Fetcher plugin. Copy this package when starting
a new integration, then replace the manifest, configuration model and fetch logic.

See the [plugin development guide](../../docs/plugin-development.md) and [development setup](../../docs/getting-started.md).
