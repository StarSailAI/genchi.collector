# 用户系统验收

状态：2026-09-09 本地实现与验收完成，并已按用户要求部署到生产环境。

## 自动化结果

- 后端 `ruff check .` 通过；完整 `pytest -q` 在本地 PostgreSQL 临时 schema 上执行，168 项通过。现有 FastAPI / aiohttp / Alembic 弃用提示仍存在，不影响结果。
- 13 项认证专项测试覆盖：邮箱规范化、六位验证码摘要存储、无提前开户、单次消费、重发替换、过期、五次错误持久化、SMTP 失败失效、来源伪造、跨实例 / 并发限流、全局小时 / 日配额、并发唯一账户、HTTPS Cookie、会话过期、所有设备撤销、关闭注册、停用账户、CSRF、资料字段白名单、跨账户关注隔离及关键词 / 标签匹配。
- 网站 `prisma:generate`、`db:check`（1.5）、`test`（7 项）、`lint`、`format:check` 与 Docker 中的 `next build` 通过。
- 后端 normalizer / product 包与 Docker 镜像构建通过。前后端 `git diff --check` 通过。

## 实际浏览器与邮件

无头 Chromium 在本地 13000 完整执行两次：Mailpit 收码 → 邮箱首次注册 → 自动进入 Dashboard → 添加系列、关键词、标签和具体活动 → 保存昵称 → 修改提前提醒 → 取消关注 → 退出所有设备。复制会话的另一个浏览器上下文随后收到 401，控制台无运行错误。

视觉检查覆盖桌面 1440px 与手机 390px / 320px，Dashboard 无横向溢出，登录页和关注管理保持现有品牌样式。截图保存在网站被忽略的 `.runtime/auth-*.png`。测试邮箱、会话和 Mailpit 测试消息在验收后定向清理。

本地迁移前备份 `genchi` 与 `genchi_private`，增量升级到 1.5，保留已有账户和业务数据。确认网站数据库角色 `genchi_reader` 无权查询 accounts、email_challenges、sessions。已更新本地 Product API 与网站容器；采集 worker、normalizer、notifier 未启动。

## 当前边界

本地产品容器通过 `.runtime/auth-local.compose.yml` 强制使用 Mailpit。完整注册与关注流程的收信验收使用本地 Mailpit；没有向真实外部邮箱发送测试邮件，也未执行 Git push。

## 生产发布验收（2026-09-09）

- 发布编号 `accounts-20260909T082818Z`。发布前保存数据库备份并通过 `pg_restore --list` 检查，同时保留源文件、配置和容器回滚镜像。
- 产品、目录处理、通知与网站容器已切换到新版本并通过健康检查；公开 HTTPS 健康接口返回 Schema 1.5，数据库迁移为 `0008_email_code_auth`。
- 发布时发现并行进行的采集更新，最终认证镜像以当时生产产品镜像为基础叠加认证模块，逐文件确认其他产品模块保持一致。
- 共享代理签名密钥已配置，可信入口覆盖来源头，网站源端口仅监听回环地址。网站数据库角色无法读取账户、验证码和会话表。
- 真实邮件通道完成 TLS 证书、SMTP 登录 / NOOP 和发信域名验证状态检查；未发送真实测试邮件，因此不将这些检查表述为收件箱投递成功。
- 公开站点无头浏览器检查通过：未登录接口、Origin 防护、签名代理验证请求、失效 Cookie 的 Secure / HttpOnly / SameSite 清理、标签读取、个人中心登录入口、登录页回到顶部及桌面 / 手机无横向溢出。
- 衬线 `g.` 图标与 Georgia 品牌字标已上线；页眉、页脚、Hero 与登录页署名统一，保留珊瑚红 `.news`。

## 登录服务版本回退修复（2026-09-09）

生产后端在后续发布中被替换为旧登录链接实现。它对登录请求返回 `{sent:true}`，产生的旧 LOGIN 队列记录被新版通知程序取消；前端此前仅检查 HTTP 成功状态，错误进入数字验证码步骤。数据库 Schema 1.5 和普通健康检查无法识别这个应用版本差异。

发布 `auth-contract-20260909` 恢复数字验证码实现，并新增 `auth_method=email_code` 能力标记。网站健康检查、登录 / 验证代理和发送成功响应均校验该标记；发布入口在更换服务前离线检查镜像的实际验证参数与会话接口。已实际证明旧生产镜像不能通过部署检查，线上页面遇到 `{sent:true}` 也不会显示验证码已发送。

后端 ruff、168 项 PostgreSQL 隔离测试、前端 9 项测试 / 类型 / 格式检查和全部 10 个 Python 分发包构建通过。Mailpit 完整登录与四类关注流程再次通过，测试账户及邮件已定向清理。线上前后端容器健康，公开接口和桌面 / 手机输入框样式检查通过；本次未向真实外部邮箱发送测试邮件。用户自行重新获取后确认收到验证码，对应挑战已被成功使用。

修复保留当前采集镜像中的其他模块，生产配置按已有文件合并，保留独立部署的 verification-agent 服务及其配置。源文件备份、镜像回滚标签和部署记录保存在服务器的私有 release 目录。

## 品牌验证码邮件（2026-09-09）

按 Email 0.2.0 规范新增 HTML / 纯文本渲染和 SMTP multipart/alternative，视觉沿用衬线品牌字标、珊瑚红及网站主 slogan。189 项后端测试、ruff、全部 10 个 Python 分发包构建通过；模板确实包含在 product wheel 中。760 / 390 / 320 px 预览与 Mailpit 实际登录收码 / 验证均通过，测试账户及消息已清理。新模板没有自动发往真实邮箱；邮箱客户端的最终呈现以用户实际收信为准。详见 `docs/email-templates.md`。

## Agent API 与 API Key（2026-09-10）

状态：本地实现中，生产迁移和发布完成后更新。

- PostgreSQL 隔离测试覆盖完整 Key 仅创建响应返回、摘要存储、列表不回传 Secret、Scope 拒绝、账号所有权、撤销立即失效、调用审计、增量游标和 MCP 初始化 / 工具发现。
- 前端个人中心不持久化完整 Key；创建响应只保存在当前组件内存，页面刷新后不可恢复。
- FoundationServices 项目 Profile 保持可验证；API Key 因上游为 `planned/spec-only`，只作为项目自建偏离记录。
