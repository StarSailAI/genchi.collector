# allfeeds-contracts

Versioned Pydantic models shared by the AllFeeds Controller, Workers and plugins.
The protocol package deliberately contains no service or storage implementation.

See the [architecture and data boundaries](../../docs/architecture.md).
