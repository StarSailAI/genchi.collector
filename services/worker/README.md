# allfeeds-worker

Outbound-only AllFeeds Worker runtime with plugin discovery, weighted slots,
heartbeats and child-process execution with hard timeouts.

See [Worker enrollment and scaling](../../docs/operations/deployment.md#add-a-worker) and [execution invariants](../../docs/development/engineering.md#worker).
